# Source material: published as release assets (release "sources-v1"), fetched by tools/fetch_sources.sh
pdfs/*.pdf
pdfs-drive/
db/*.db

# OCR working files: the per-page records are large and regenerable; the extracted
# articles (ocr/<book>/articles.jsonl) and reports ARE committed
ocr/*/pages/
ocr/*/*.tar.gz
ocr/osbct_vocab.txt
abh_tmp/
tmp/

# local only
*.zip
.DS_Store
__pycache__/
_incoming/
release/

# Reader data: regenerable with tools/abhidhana_reader_data.py; published beside the artifact
reader/*.json
reader/*.gz.txt

# Typed witness (PCED dictionary K): licence not stated, so kept out of git (docs/witness.md §5)
witness/
logs/
reader/*.wasm
site/dist/
